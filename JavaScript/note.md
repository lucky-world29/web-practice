Q.what JS can do?
 -> web interaction
 ->DOM manipulation (Document Object Model)
    => window.document  {
      window.document.createElement('button')
      btn.innerHTML="signUp"
      document.body.appendChild(btn)
    }
 ->server-side(node.js)   
 ->event handling
 ->game app(phraseJS) 
 ->Mobile App(react native)
 ->browser controlling from js
 ->database(MongoDB)

    

    =============================

 => variables

 const
 var
 let
    